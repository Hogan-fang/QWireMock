# QWire Mock - Production dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Test
pytest>=7.0
httpx>=0.27.0

# MySQL (order persistence; MySQL 8 needs cryptography for caching_sha2_password)
PyMySQL>=1.1.0
cryptography>=41.0.0

# Optional local file DB
sqlalchemy>=2.0.0
tinydb>=4.0.0
