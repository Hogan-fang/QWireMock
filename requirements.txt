# QWire Mock - Production dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Test
pytest>=7.0

# MySQL（订单持久化，MySQL 8 需 cryptography 支持 caching_sha2_password）
PyMySQL>=1.1.0
cryptography>=41.0.0

# 本地文件数据库（可选）
sqlalchemy>=2.0.0
tinydb>=4.0.0
